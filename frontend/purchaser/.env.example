# Copy to `.env.local` to override defaults.

# Base URL of the verifier backend (no trailing slash). The purchaser POSTs a
# whole chain to {VITE_BACKEND_URL}/verify.
VITE_BACKEND_URL=http://localhost:8000
